export interface IInputCustom {
    name: string,
    title: string,
    type: "amount" | "price" | "number" | 'password',
    btn?: any
}

export interface ItemsTab {
    id: number,
    title: string,
}

